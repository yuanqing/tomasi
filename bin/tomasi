#!/usr/bin/env node

'use strict';

var tomasi = require('..');

var error = function(err) {
  console.error(err);
  process.exit(1);
};

tomasi('tomasi.js', function(err) {
  if (err) {
    return error(err);
  }
  process.exit(0);
});
